package io.book;

import java.io.Console;
import java.math.BigInteger;

/**
 * @author jon poulton
 */
public class HomeWork {

    // Enter integer and print squares of that integer from 1 to that integer
//    public static void main(String[] args) {
//        Console console = System.console();
//        String input = console.readLine("Please enter a number between 1 and 10");
//        int max = Integer.parseInt(input);
//        for (int i = 1; i < max; i++) {
//            console.printf("%d\n", i*i);
//        }
//    }

    // Problem one, misses one number, by excluding the last number
    // Does not handle values of 0 or less
    // Throws exception for non-numbers
    // Problem statement does not include the number 10 as a limit - and does not impose 10 as a limit anyway
    // Does not handle integer overflow

    // Enter integer and print squares of that integer from 1 to that integer
//    public static void main(String[] args) {
//        Console console = System.console();
//        String input = console.readLine("Please enter an integer: ");
//        BigInteger integer = new BigInteger(input);
//        if (integer.min(BigInteger.ONE)) {
//
//        } else {
//
//        }
//        for (int i = 1; i < max; i++) {
//            console.printf("%d\n", i*i);
//        }
//    }
}
